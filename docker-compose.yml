version: "3.8"

services:
  mongo:
    image: mongo:7
    container_name: mongo
    restart: always
    ports:
      - "27017:27017"

  server:
    build: .
    container_name: bdcalling-academy-server
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - mongo
